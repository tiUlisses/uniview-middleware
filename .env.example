# Uniview LiteAPI - exemplo de variáveis de ambiente
# Copie para .env e ajuste conforme o seu ambiente.

# --- Autenticação / conexão ---
# Base URL da câmera (use um IP/host válido no seu ambiente)
UNV_BASE_URL=http://192.0.2.10
# Usuário da câmera
UNV_USER=admin
# Senha da câmera
UNV_PASS=changeme

# --- Receiver (listener HTTP para notificações) ---
# Host do listener (para receber notificações)
RECEIVER_HOST=0.0.0.0
# Porta do listener
RECEIVER_PORT=8080

# Tag e categoria para normalização do payload
EVENT_TAG=uniview
EVENT_CATEGORY=event

# --- Normalização de AlarmType ---
# JSON inline com mapeamento AlarmType -> {"ivs_type":"...","message":"..."}
ALARM_TYPE_MAPPING_JSON={"Motion":{"ivs_type":"motion","message":"Motion detected"}}
# Caminho para arquivo JSON com o mesmo formato (tem precedência sobre o JSON inline)
ALARM_TYPE_MAPPING_FILE=examples/alarm_type_mapping.json

# --- Forwarding (encaminhar eventos recebidos) ---
# URL completa para POST (opcional, alternativa ao host/path)
FORWARD_URL=http://localhost:9000/webhooks/uniview
# Esquema (default http)
FORWARD_SCHEME=http
# Host do destino (se não usar FORWARD_URL)
FORWARD_HOST=localhost
# Porta do destino (opcional)
FORWARD_PORT=9000
# Path do destino (default /)
FORWARD_PATH=/webhooks/uniview

# --- Subscription / keepalive ---
# Duração da subscription (segundos)
DURATION=60
# Máscara de eventos (conforme PDF da Uniview)
TYPE_MASK=97663
# Modo de push de imagens (conforme PDF da Uniview)
IMAGE_PUSH_MODE=0
# ID da subscription (necessário para keepalive/unsubscribe)
SUBSCRIPTION_ID=

# --- Payloads (não inventar campos; use templates) ---
# JSON inline para subscribe (alternativa ao arquivo)
SUBSCRIBE_PAYLOAD=
# Caminho para o template JSON de subscribe
SUBSCRIBE_PAYLOAD_FILE=examples/subscribe_payload_template.json
# JSON inline para keepalive (alternativa ao arquivo)
KEEPALIVE_PAYLOAD=
# Caminho para o template JSON de keepalive
KEEPALIVE_PAYLOAD_FILE=examples/keepalive_payload_template.json

# --- CSV de câmeras ---
# Caminho para CSV com múltiplas câmeras
CAMERA_CSV_FILE=examples/cameras.csv
